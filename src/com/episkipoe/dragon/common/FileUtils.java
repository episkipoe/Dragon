package com.episkipoe.dragon.common;

//import java.io.File;
//import java.io.FileOutputStream;
//import java.io.ObjectOutputStream;
//import java.io.Serializable;
//import java.util.List;

public class FileUtils {
	/*
	public static void writeToFile(File fileName, List<Serializable> objects) throws Exception {
		FileOutputStream fos=null;
		ObjectOutputStream oos=null;
		try {
			fos = new FileOutputStream(fileName);
			oos = new ObjectOutputStream(fos);
			for(Object o : objects) oos.writeObject(o);
		} finally {
			if(oos!=null) oos.close();
		} 
	}
	public static void writeToFile(File fileName, Serializable object) throws Exception {
		FileOutputStream fos=null;
		ObjectOutputStream oos=null;
		try {
			fos = new FileOutputStream(fileName);
			oos = new ObjectOutputStream(fos);
			oos.writeObject(object);
		} finally {
			if(oos!=null) oos.close();
		} 
		
	}
	*/
}
